name: Manual CleanX OpenWrt package build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    name: Use pre-release workflow
    runs-on: ubuntu-24.04
    steps:
      - name: Notice
        run: |
          echo "Automatic package builds are handled by .github/workflows/build-prerelease.yml"
          echo "Run 'Build CleanX pre-release packages' for APK/IPK builds and pre-release assets."
