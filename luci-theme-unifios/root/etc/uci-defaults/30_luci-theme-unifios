#!/bin/sh
# luci-theme-unifios — register the theme and make it the active LuCI theme.
# Runs once on install; uci-defaults removes the script afterwards.

uci -q batch <<-EOF
	set luci.themes.UniFiOS=/luci-static/unifios
	set luci.main.mediaurlbase=/luci-static/unifios
	commit luci
EOF

# Clear any cached compiled templates so the new theme is picked up immediately.
rm -f /tmp/luci-indexcache /tmp/luci-modulecache/* 2>/dev/null
rm -rf /tmp/luci-modulecache 2>/dev/null

exit 0
